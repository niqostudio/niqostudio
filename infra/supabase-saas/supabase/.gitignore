# Supabase
.branches
.temp

# JWT 署名鍵（秘密鍵を含む＝絶対にコミットしない）
signing_keys.json

# dotenvx
.env.keys
.env.local
.env.*.local

# billing 関数の正本は saas-platform/functions（ここへは serve 時にリンクを張る）。追跡しない。
functions/
